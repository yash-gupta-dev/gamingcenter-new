export enum USER_AUTH_PROVIDERS {
    EMAIL = "EMAIL",
    GOOGLE = "GOOGLE",
}

export enum PRIORITY {
    PRIORITY = "PRIORITY",
    CASUAL = "CASUAL",
    MUTED = "MUTED"
}

export enum STATUS {
    PENDING = "PENDING",
    ACCEPTED = "ACCEPTED",
    REJECTED = "REJECTED"
}

export enum EVENT_STATUS {
    UPCOMING = "UPCOMING",
    LIVE = "LIVE",
    COMPLETED = "COMPLETED",
    CANCELLED = "CANCELLED",
}

export enum ROOM_TYPE {
    PUBLIC = "PUBLIC",
    PRIVATE = "PRIVATE",
}